import React from 'react'

// FUNCTIONALITY
// - render the next page/view --> useNavigate??

// later
// - store data in database (via axios)

export default function NextButton() {
    return (
        <button>
            NEXT
        </button>
    )
}
